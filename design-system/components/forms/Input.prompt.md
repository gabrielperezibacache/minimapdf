Text field for search, filename rename, and settings forms.

```jsx
<Input placeholder="Search library…" icon={<SearchIcon />} />
```

Focus state swaps the border to bronze — no glow/shadow. Background matches surface tone so fields sit flush inside sidebars.
