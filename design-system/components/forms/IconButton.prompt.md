Square icon-only button for toolbars, sidebars, and the tool drawer (zoom, bookmark, search, page-nav).

```jsx
<IconButton title="Bookmark" active><BookmarkIcon /></IconButton>
```

`active` shows a bronze border + tinted fill (used for the current tool/tab). Pass any icon glyph as children — usually a Lucide `<svg>`.
