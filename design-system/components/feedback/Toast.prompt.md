Transient confirmation banner ("Document saved offline", "Sync failed").

```jsx
<Toast tone="success">Library synced locally</Toast>
```

Flat surface + border only, no drop shadow, matches the zero-shadow rule.
