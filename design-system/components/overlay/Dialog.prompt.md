Modal for confirmations and small forms (delete document, rename, purchase unlock).

```jsx
<Dialog open title="Delete document?" onClose={close}>This can't be undone.</Dialog>
```

Sharp 8px radius surface, 1px border, no shadow — dimmed scrim behind.
