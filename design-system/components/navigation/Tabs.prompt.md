Underline tab switcher for the tool drawer (Notes / Bookmarks / Outline) and settings.

```jsx
<Tabs items={[{label:"Outline",value:"outline"},{label:"Bookmarks",value:"bookmarks"}]} value="outline" onChange={setTab} />
```
